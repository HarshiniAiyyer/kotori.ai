langchain==0.3.7
langchain_chroma==0.1.4
langchain_community==0.3.7
langchain_ollama==0.2.0
langchain_text_splitters==0.3.2

#how i did it. first i cd into the repo and then pip install pipreqs and then after that pipreqs . --force  
